# 06-06-2026

-Started the fastapi torturial then where i learnt the following:
*used uv as package manager
*downloaded the fastapi using: 'uv add "fastapi[standard]"' which holds all needed things of the framework
*Learnt about creating routes, docs, redoc, stacking decorators, hiding or removing html response from the api docs using include_scheme method

## 08-06-2026

*learnt about using Jinja2 syntax, webmanifest, Jinja2 inheritance, using name method in the decorators which allows url to point to the exact page and not be as one, using "kurl_for" which allows flexible path generation and it's automatic
